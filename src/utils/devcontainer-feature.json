{
    "name": "My custom utils",
    "id": "utils",
    "version": "0.0.2",
    "description": "This installs all my custom utils for my workflow",
    "options": {
        "upgradePackages": {
            "type": "boolean",
            "default": true,
            "description": "Upgrade OS packages?"
        },
        "installFD": {
            "type": "boolean",
            "default": true,
            "description": "Install Ripgrep?"
        },
        "installRG": {
            "type": "boolean",
            "default": true,
            "description": "Install Ripgrep?"
        },
        "installTMUX": {
            "type": "boolean",
            "default": true,
            "description": "Install tmux?"
        }
    }
}
